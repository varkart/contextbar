schema_version = 1
id = "opencode"
name = "OpenCode"
skills_agent_name = "opencode"

[[detection]]
type = "dir"
path = "~/.config/opencode"

[[detection]]
type = "binary"
name = "opencode"

[version]
type = "command"
binary = "opencode"
args = ["--version"]
timeout_ms = 1500
parse = "last_token"

# MCP servers live under the "mcp" key in opencode.json; each entry toggles
# itself off via its own "enabled": false rather than moving to a separate section.
[[mcp_sources]]
id = "opencode_json"
type = "json_key_pair"
file = "~/.config/opencode/opencode.json"
active_key = "mcp"
inline_toggle_field = "enabled"
jsonc = true

# OpenCode-native skills
[[skill_sources]]
id = "skills_dir"
type = "directory"
path = "~/.config/opencode/skills"
disabled_subdir = ".disabled"

# Skills shared across AI tools (Claude, Gemini, Codex, …)
[[skill_sources]]
id = "agents_skills_dir"
type = "directory"
path = "~/.agents/skills"
disabled_subdir = ".disabled"
