{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and expanded windows",
  "windows": ["main", "expanded"],
  "permissions": [
    "core:default",
    "opener:default",
    "notification:default"
  ]
}
