name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g. v0.8.0)'
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve tag name
        id: ref
        run: |
          TAG="${{ github.event.inputs.tag || github.ref_name }}"
          echo "tag=$TAG" >> $GITHUB_OUTPUT

      - name: Extract release notes from tag annotation
        id: tag
        run: |
          git fetch --tags --force
          NOTES=$(git tag -l --format='%(contents)' "${{ steps.ref.outputs.tag }}")
          if [ -z "$NOTES" ]; then
            echo "notes=No release notes provided." >> $GITHUB_OUTPUT
          else
            {
              echo "notes<<EOF"
              echo "$NOTES"
              echo "EOF"
            } >> $GITHUB_OUTPUT
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin,x86_64-apple-darwin

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install dependencies
        run: npm ci

      - name: Build universal app
        run: npm run tauri build -- --target universal-apple-darwin
        env:
          VITE_PUBLIC_POSTHOG_PROJECT_TOKEN: ${{ secrets.VITE_PUBLIC_POSTHOG_PROJECT_TOKEN }}
          VITE_PUBLIC_POSTHOG_HOST: ${{ secrets.VITE_PUBLIC_POSTHOG_HOST }}
          VITE_SENTRY_DSN: ${{ secrets.VITE_SENTRY_DSN }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: ${{ secrets.SENTRY_ORG }}
          SENTRY_PROJECT: ${{ secrets.SENTRY_PROJECT }}

      - name: Ad-hoc sign
        run: |
          APP=$(find src-tauri/target/universal-apple-darwin/release/bundle/macos -name "*.app" -maxdepth 1 | head -1)
          echo "Signing: $APP"
          codesign --deep --force --sign - "$APP"

      - name: Package DMG
        run: |
          VERSION="${{ steps.ref.outputs.tag }}"
          APP=$(find src-tauri/target/universal-apple-darwin/release/bundle/macos -name "*.app" -maxdepth 1 | head -1)
          APP_NAME=$(basename "$APP" .app)
          DMG="${APP_NAME}_${VERSION}_universal.dmg"
          STAGING="/tmp/llmmanager_dmg"
          mkdir -p "$STAGING"
          cp -r "$APP" "$STAGING/"
          ln -s /Applications "$STAGING/Applications"

          # Install note shown alongside the app in the DMG
          cat > "$STAGING/INSTALL.txt" << 'INSTALLEOF'
          Drag LLM Manager to Applications, then launch it.

          If macOS blocks the app on first launch, run this once in Terminal:

              xattr -d com.apple.quarantine /Applications/LLM Manager.app

          Or: System Settings → Privacy & Security → Open Anyway
          INSTALLEOF

          hdiutil create \
            -volname "${APP_NAME} ${VERSION}" \
            -srcfolder "$STAGING" \
            -ov -format UDZO \
            -o "$DMG"
          echo "DMG_NAME=$DMG" >> $GITHUB_ENV

      - name: Compute DMG SHA256
        run: |
          SHA256=$(shasum -a 256 "${{ env.DMG_NAME }}" | awk '{print $1}')
          echo "DMG_SHA256=$SHA256" >> $GITHUB_ENV

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "LLM Manager ${{ steps.ref.outputs.tag }}"
          body: |
            ${{ steps.tag.outputs.notes }}

            ---
            **Install (DMG):** Download the `.dmg` below.
            **Install (Homebrew):** `brew tap varkart/tap && brew install --cask llmmanager`
            **macOS security prompt:** run `xattr -d com.apple.quarantine /Applications/LLM\ Manager.app`
          files: ${{ env.DMG_NAME }}
          draft: false
          prerelease: false

      - name: Update Homebrew tap
        if: ${{ secrets.HOMEBREW_TAP_TOKEN != '' }}
        env:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          VERSION="${{ steps.ref.outputs.tag }}"
          VERSION_NUM="${VERSION#v}"
          SHA256="${{ env.DMG_SHA256 }}"

          git clone "https://x-access-token:${HOMEBREW_TAP_TOKEN}@github.com/varkart/homebrew-tap.git" /tmp/homebrew-tap
          cd /tmp/homebrew-tap

          sed -i '' "s/version \".*\"/version \"${VERSION_NUM}\"/" Casks/llmmanager.rb
          sed -i '' "s/sha256 \".*\"/sha256 \"${SHA256}\"/" Casks/llmmanager.rb

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/llmmanager.rb
          git commit -m "chore: bump llmmanager to ${VERSION}"
          git push
